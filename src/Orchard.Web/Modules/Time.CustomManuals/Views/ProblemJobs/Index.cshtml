@model IEnumerable<Time.Data.EntityModels.CustomManuals.ProblemJob>

@{
//ViewBag.Title = "Open Problem Jobs";
}

<div class="container">
    <div class="row">
        @Html.Partial("_CustomManualSideBar")

        <div class="col-md-8 col-sm-8 col-xs-8">
            <div class="row">
                <div class="col-md-10"><h4>@ViewBag.Title</h4><hr /></div>
            </div>
            @foreach (var item in Model)
            {
                @Html.Partial("_ProblemJob", item)
            }
        </div>
    </div>
</div>