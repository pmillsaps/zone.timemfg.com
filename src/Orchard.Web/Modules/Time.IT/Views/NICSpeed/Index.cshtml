@model IEnumerable<Time.Data.EntityModels.ITInventory.Ref_NICSpeed>

@{
    ViewBag.Title = "NIC Speed Index";
}

<div class="container">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h2>@ViewBag.Title</h2>
        </div>
        <div class="panel-body">
            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <hr />

            <table class="table table-striped table-bordered table-condensed table-hover">
                <tr class="success">
                    <th>
                        @Html.DisplayNameFor(model => model.NIC_Speed)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.ActionLink(item.NIC_Speed, "Edit", new { id = item.Id })
                        </td>
                        <td>
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>

